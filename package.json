{
  "name": "@lekman/mmd",
  "version": "1.2.1",
  "description": "Mermaid diagram management CLI — extract, render, and inject themed SVGs into Markdown",
  "module": "src/cli/index.ts",
  "type": "module",
  "bin": {
    "mmd": "./dist/index.js"
  },
  "files": [
    "dist",
    "src/templates"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lekman/mmd.git"
  },
  "author": "Tobias Lekman",
  "license": "MIT",
  "packageManager": "bun@1.3.7",
  "scripts": {
    "start": "bun run src/cli/index.ts",
    "lint": "biome check src/ tests/",
    "lint:fix": "biome check --fix src/ tests/",
    "format": "biome format --write src/ tests/",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests/unit/",
    "test:coverage": "bun test tests/unit/ --coverage --coverage-reporter=lcov --coverage-reporter=text",
    "build": "bun build src/cli/index.ts --outdir dist --target bun",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "@northbridge-security/ai-toolkit": "^0.1.12",
    "@types/bun": "^1.3.7",
    "husky": "^9.1.7",
    "turbo": "^2.8.0",
    "typescript": "^5.7.3"
  },
  "optionalDependencies": {
    "@mermaid-js/mermaid-cli": "^11.4.2"
  },
  "dependencies": {
    "beautiful-mermaid": "^0.1.3",
    "commander": "^14.0.2"
  }
}
